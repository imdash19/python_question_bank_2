# Write a Python program to store your name in a string variable.
# Print the original string value.
# Use the len() function to find the length of the string.
# Use the type() function to display the data type.
# Ensure all outputs are printed in a readable format.

name= input()
print(f'''Original String: {name}
Length of String: {len(name)}
Type: {type(name)}''')
