# Write a Python program to read a list of elements.
# Use a for loop to iterate through each element of the list.
# Print each element on a separate line inside the loop.
# Ensure all list elements are displayed clearly in order.
# This demonstrates basic list traversal using loops.

elements = input().split()

for element in elements:
    print(element)
