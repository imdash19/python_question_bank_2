# Write a Python program to create a set containing numbers from 1 to 5.
# The program should display the set elements.
# It should also display the total number of elements present in the set.
# Finally, the program must print the data type of the created set.
# No input is required from the user.
# The output should be displayed in a single readable line.

my_set= {1, 2, 3, 4, 5}
print(f'Set: {my_set}, Length: {len(my_set)}, Type: {type(my_set)}')
