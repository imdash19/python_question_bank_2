# Write a Python program to accept a string from the user.
# Use string slicing to extract only the first five characters.
# Ensure the program handles strings with fewer than five characters safely.
# Store the sliced part in a variable.
# Print the extracted characters clearly as output.

s= input()
print(s[:5])
