# Write a Python program to store student name and marks.
# Use the .format() method to insert values into a string.
# Combine text and variables neatly.
# Print the formatted output.

name = input()
marks = int(input())

print("Student {} scored {} marks.".format(name, marks))
