# Write a Python program to accept a sentence as input.
# Use the replace() method to replace all spaces with hyphens (-).
# Ensure every space in the string is replaced.
# Store the modified string in a variable.
# Print the updated string clearly.

sentence = input()

modified_sentence = sentence.replace(" ", "-")

print(modified_sentence)
