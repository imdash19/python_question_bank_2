# Write a Python program to take first name and last name as input.
# Store both values in separate variables.
# Use an f-string to combine them into a full name.
# Print the full name.

first_name = input()
last_name = input()

full_name = f"{first_name} {last_name}"

print(full_name)
