# Write a Python program to calculate the factorial of a number.
# The program should take a non-negative integer as input.
# Use the math.factorial() function for calculation.
# The result should be displayed clearly.
# The program must handle edge cases like 0 or 1.
# Dot notation must be used to call the factorial function.

import math
print(math.factorial(int(input())))
