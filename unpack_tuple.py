# Write a Python program to assign elements of a tuple to separate variables.
# Create a tuple with 3 values 10 20 30
# Each element should be assigned to a distinct variable.
# Print the variables individually with descriptive labels.
# Ensure the number of variables matches the number of elements.

numbers = (10, 20, 30)

a, b, c = numbers

print("a =", a)
print("b =", b)
print("c =", c)
