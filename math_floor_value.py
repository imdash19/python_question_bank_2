# Write a Python program that calculates the floor value of a float number.
# Input is provided by the user.Use math.floor() to compute the largest integer less than 
# or equal to the number.The program should print the result as an integer.
# Dot notation must be used to call the function.The program should handle
# both positive and negative numbers.

import math
print(math.floor(float(input())))
