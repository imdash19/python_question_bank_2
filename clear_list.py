# Write a Python program to accept a list from the user.
# Use the clear() method to remove all elements.
# Store the cleared list.
# Print the empty list clearly.

lst= list(input().split())
lst.clear()
print(lst)
