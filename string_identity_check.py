# Write a Python program to read two strings with the same text.
# Use the is operator to compare both strings.
# Check whether Python stores them in the same memory location.
# If both refer to the same object, print True.
# Otherwise, print False.

s1, s2= input(), input()
print(s1 == s2)
