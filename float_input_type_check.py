# Write a Python program to accept a float number as input.
# Store the value in a variable.
# Use the type() function to identify its data type.
# Print the result to confirm it is a float type.

n= float(input())
print(type(f))
