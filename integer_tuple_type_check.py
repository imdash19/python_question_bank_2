# Write a Python program to create a tuple containing only integers.
# Store the numbers in a tuple variable.
# Use the type() function to check its data type.
# Print the type to understand immutable data structures in Python.

tup= (10, 20, 30, 40, 50)
print(type(tup))
