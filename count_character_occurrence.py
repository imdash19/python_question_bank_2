# Write a Python program to accept a string.
# Choose a character whose occurrence needs to be counted.
# Use the count() method to find how many times it appears.
# Store the count in a variable.
# Print the character and its total count.

s= input()
v= input()
print(s.count(v))
