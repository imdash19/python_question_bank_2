# Write a Python program to create a list containing repeated elements.
# Convert the list into a set using the set() function.
# Observe that duplicate values are automatically removed.
# Print the resulting set.

lst= [1, 2, 3, 2, 3, 4, 4]
lst= set(lst)
print(lst)
