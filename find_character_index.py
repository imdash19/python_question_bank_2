# Write a Python program to accept a string input.
# Specify a character to search within the string.
# Use the find() method to locate its first occurrence.
# Store the returned index value.
# Print the index position clearly.

s, v= input(), input()
print(s.find(v))
