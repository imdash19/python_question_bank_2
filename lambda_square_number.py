# Write an anonymous function to calculate the square of a number.
# The input is a single integer.
# Do not use a named function.
# Use a lambda expression for calculation.
# Return the square value.
# Read input from the console.
# Print the output.
# Ensure correct multiplication logic.

square = lambda x: x * x

number = int(input())
print(square(number))
