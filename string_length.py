# Write a Python program to accept a string value.
# Use the built-in len() function to calculate the length of the string.
# Ensure that spaces are also counted as characters.
# Store the length in a variable.
# Print the final length clearly as output.

s= input()
print(len(s))
