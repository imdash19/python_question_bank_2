# Write a Python program to extract a portion of a tuple.
# Use slicing to select elements from index 1 up to 4 (excluding 4).
# Store the sliced elements in a new tuple.
# Print the resulting tuple.

tup= tuple(input().split())
print(tup[1:4])
