# Write a Python program that takes a word from the user and calculates how many characters (letters) are in that word. Your program should use the len() function to count the total number of characters and display the result.
# For example, if the user enters "python", your program should display "6" because the word "python" has 6 letters.
# Input Format:
# A single word (no spaces)
# Output Format:
# A single number representing the total count of characters in the word

s= input()
prit(len(s))
