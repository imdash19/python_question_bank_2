# Write an anonymous function to add two numbers.
# The inputs are provided in one line separated by space.
# Use a lambda function with two parameters.
# Do not define a named function.
# Return the sum of the two numbers.
# Print the output.
# Handle negative values correctly.
# Ensure correct input parsing.

two_sum= lambda x, y: x+y
print(two_sum(int(input()), int(input())))
