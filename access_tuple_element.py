# Write a Python program to access an element at a specific position in a tuple.
# Use indexing to select the third element (index 2).
# Ensure the tuple has enough elements to avoid errors.
# Print the accessed element clearly.

data = ("Python", 3.14, 25, True, "Hello")

third_element = data[2]

print(third_element)
