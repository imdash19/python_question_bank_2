# Write a Python program that takes two numbers from the user, adds them together, and displays the result. The user will enter the first number, then the second number on separate lines. Your program should calculate their sum and show the answer.
# For example, if the user enters "10" and "20", your program should display "30".
# Input Format:
# Two lines, each containing one integer number
# Output Format:
# A single number representing the sum of the two input numbers

n1, n2= int(input()), int(input())
print(n1+n2)
