# Write a Python program to calculate x raised to the power y.
# The program should take two inputs: base x and exponent y.Use the math.pow() function.
# The program should display the result as a float.Dot notation must be used to call the function.
# The program should handle positive, negative, and zero exponents.

import math
print(math.pow(int(input()), int(input())))
