# Write a Python program to accept a full name from the user as input.
# Use a loop to go through each character in the string one by one.
# Print every character on a separate line.
# Ensure spaces in the name are also handled correctly.
# Display the output clearly, with one character per line.

name= input()

for v in name:
    print(v)
