# Write a Python program to accept two whole numbers as input.
# Store both values in separate variables.
# Add the two integer values using the addition operator.
# Display the final sum as output.

n1, n2= int(input()), int(input())
print(s1+s2)
