# Write a Python program to count occurrences of a character in a string.
# Accept a string and a character from the user.
# Use a for loop to iterate through the string.
# Compare each character with the given input character.
# Print the total count of occurrences.

s= input()
c= input()
print(s.count(c))
