# Write a Python program to store a Boolean value (True or False).
# Use the type() function to determine the data type.
# Learn how Python handles Boolean values internally.
# Display the Boolean type as the output.

val= True
print(type(val))
