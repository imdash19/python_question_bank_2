# Write a Python program to assign a text value to a variable.
# Use the type() function to check the variable’s data type.
# Understand how Python treats text as a string.
# Print the output showing the string data type.

s= input()
print(type(s))
