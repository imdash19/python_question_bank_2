# Write a Python program to take height as a float value.
# Format the float to show exactly 3 decimal places.
# Use formatting techniques to control precision.
# Print the formatted height.

height = float(input())

print("{:.3f}".format(height))
