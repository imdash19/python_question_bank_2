# Write a Python program to reverse the elements of a tuple.
# create a tuple with values 10 20 30 Then
# Use slicing with a negative step ([::-1]) to reverse.
# Store the reversed tuple in a new variable.
# Print the reversed tuple clearly.

numbers = (10, 20, 30)

reversed_tuple = numbers[::-1]

print(reversed_tuple)
