# Write a Python program to store a decimal number in a variable.
# Use the type() function to identify its data type.
# Observe how Python recognizes floating-point values.
# Print the detected data type on the screen.

n= float(input())
print(type(n))
