# Write a Python program to accept a string from the user.
# Use the .upper() method to convert the string to uppercase.
# Use the .lower() method to convert the string to lowercase.
# Print both converted results separately.
# Ensure the original string is not modified permanently.

s= input()
print(f'''Uppercase: {s.upper()}
Lowercase: {s.lower()}''')
